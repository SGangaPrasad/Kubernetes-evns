#!/bin/bash
set -e

# Create the namespace required for the scenario
kubectl create namespace project-hamster --dry-run=client -o yaml | kubectl apply -f -

echo "✅ Environment setup complete! Namespace project-hamster is ready."
